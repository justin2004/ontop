jdbc.name = database_conference
jdbc.url = jdbc:mysql://${docker.url}:${docker.port}/conference?useSSL=false
jdbc.user = root
jdbc.password = ${docker.mysql.password}
jdbc.driver = com.mysql.jdbc.Driver

