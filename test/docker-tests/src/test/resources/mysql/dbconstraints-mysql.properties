jdbc.name = oracleBolzano
jdbc.url = jdbc:mysql://${docker.url}:${docker.port}/dbconstraints?useSSL=false
jdbc.user = root
jdbc.password = ${docker.mysql.password}
jdbc.driver = com.mysql.jdbc.Driver
