jdbc.name	= Books
#jdbc.url = jdbc:mysql://${docker.url}:${docker.port}/books?sessionVariables=sql_mode='ANSI'
jdbc.url = jdbc:mysql://${docker.url}:${docker.port}/books?useSSL=false
jdbc.user = root
jdbc.password = ${docker.mysql.password}
jdbc.driver = com.mysql.jdbc.Driver